<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="72dp"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="6dp"
    android:gravity="center_horizontal"
    android:orientation="vertical">

    <FrameLayout
        android:layout_width="56dp"
        android:layout_height="56dp">

        <ImageView
            android:id="@+id/ivStoryAvatarPhoto"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@drawable/bg_circle_ring_purple"
            android:contentDescription="Member Photo"
            android:scaleType="centerCrop"
            android:visibility="gone" />

        <TextView
            android:id="@+id/tvStoryAvatarInitials"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@drawable/bg_purple_button"
            android:fontFamily="sans-serif-black"
            android:gravity="center"
            android:text="J"
            android:textColor="@color/white"
            android:textSize="19sp"
            android:textStyle="bold" />

        <!-- Camera Edit Photo Badge -->
        <FrameLayout
            android:id="@+id/badgeStoryCameraEdit"
            android:layout_width="18dp"
            android:layout_height="18dp"
            android:layout_gravity="bottom|end"
            android:background="@drawable/bg_pill_purple_solid">

            <ImageView
                android:layout_width="10dp"
                android:layout_height="10dp"
                android:layout_gravity="center"
                android:contentDescription="Change Photo"
                android:src="@drawable/ic_camera"
                app:tint="@color/white" />
        </FrameLayout>

    </FrameLayout>

    <TextView
        android:id="@+id/tvStoryMemberName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="5dp"
        android:ellipsize="end"
        android:fontFamily="sans-serif-medium"
        android:gravity="center"
        android:maxLines="1"
        android:text="John"
        android:textColor="@color/text_primary"
        android:textSize="11.5sp" />

    <TextView
        android:id="@+id/tvStoryMemberRole"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:ellipsize="end"
        android:gravity="center"
        android:maxLines="1"
        android:text="Editor"
        android:textColor="@color/purple_primary"
        android:textSize="9.5sp"
        android:textStyle="bold" />

</LinearLayout>
